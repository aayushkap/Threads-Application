export default async function page() {
    return(
        <main>
            <h1 className="head-text">
                This is the page inside onboarding which is inside (auth)
            </h1>
        </main>
    )
}
    
